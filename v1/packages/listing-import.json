{
    "author": "Navjot Tomer (Mindstellar)",
    "author_uri": "https://mindstellar.com",
    "categories": [
        "admin-tools"
    ],
    "description_html": "<h1>Listing Import</h1>\n<p>Import listings into Shopclass from other systems: a keyed REST API, and scheduled JSON, CSV and RSS feeds. Every listing goes through the same checks as one posted by hand: moderation, spam checks, listing limits and expiry.</p>\n<p><img src=\"https://raw.githubusercontent.com/mindstellar/shopclass-plugin-listing-import/v0.2.0/assets/screenshot-1.png\" alt=\"Import sources\" loading=\"lazy\"></p>\n<table><tbody><tr><td><img src=\"https://raw.githubusercontent.com/mindstellar/shopclass-plugin-listing-import/v0.2.0/assets/screenshot-2.png\" alt=\"A feed source\" loading=\"lazy\"></td><td><img src=\"https://raw.githubusercontent.com/mindstellar/shopclass-plugin-listing-import/v0.2.0/assets/screenshot-3.png\" alt=\"Feed preview\" loading=\"lazy\"></td></tr><tr><td>A feed source</td><td>Preview a feed before it changes anything</td></tr><tr><td><img src=\"https://raw.githubusercontent.com/mindstellar/shopclass-plugin-listing-import/v0.2.0/assets/screenshot-4.png\" alt=\"API keys\" loading=\"lazy\"></td><td><img src=\"https://raw.githubusercontent.com/mindstellar/shopclass-plugin-listing-import/v0.2.0/assets/screenshot-5.png\" alt=\"Help\" loading=\"lazy\"></td></tr><tr><td>API keys with their own permissions</td><td>A help screen with the API guide</td></tr></tbody></table>\n<h2>Requirements</h2>\n<ul><li>Shopclass 6.4.0 or later</li><li>PHP 8.0 or later</li></ul>\n<h2>Install</h2>\n<p>From the admin: <strong>Plugins → Manage plugins → Browse</strong>, find <em>Listing Import</em>, then <strong>Install</strong>. Or from the command line:</p>\n<pre><code>php oc-cli.php market:install listing-import</code></pre>\n<h2>Check that it works</h2>\n<pre><code>curl https://example.com/api/v1/ping</code></pre>\n<p>With friendly URLs off, use the query form:</p>\n<pre><code>curl &quot;https://example.com/index.php?page=route&amp;route=listing-import-api&amp;path=ping&quot;</code></pre>\n<p>Both answer:</p>\n<pre><code>{&quot;data&quot;:{&quot;plugin&quot;:&quot;listing-import&quot;,&quot;version&quot;:&quot;0.2.0&quot;}}</code></pre>\n<h2>Make a key</h2>\n<p><strong>Plugins → Listing import → API keys → Add a key.</strong> Pick its permissions:</p>\n<table><thead><tr><th>Permission</th><th>Lets the key</th></tr></thead><tbody><tr><td><code>listings:write</code></td><td>import listings, and read where one stands</td></tr><tr><td><code>listings:delete</code></td><td>delete a listing it imported</td></tr><tr><td><code>runs:read</code></td><td>read how a batch went</td></tr></tbody></table>\n<p>The key is shown once. Keep it safe. Send it on every request:</p>\n<pre><code>Authorization: Bearer &lt;key&gt;</code></pre>\n<p>Or make one from the command line:</p>\n<pre><code>php oc-cli.php import:key:create --name=&quot;Partner site&quot; --scopes=listings:write,runs:read</code></pre>\n<h2>Send listings</h2>\n<table><thead><tr><th>Request</th><th>What it does</th></tr></thead><tbody><tr><td><code>POST /api/v1/listings</code></td><td>Import one record now.</td></tr><tr><td><code>PUT /api/v1/listings/{external_id}</code></td><td>The same, with the id in the address.</td></tr><tr><td><code>GET /api/v1/listings/{external_id}</code></td><td>The listing this id became, its address, and if it is live.</td></tr><tr><td><code>DELETE /api/v1/listings/{external_id}</code></td><td>Delete that listing.</td></tr><tr><td><code>POST /api/v1/listings:batch</code></td><td>Up to 200 records, imported in the background.</td></tr><tr><td><code>GET /api/v1/runs/{id}</code></td><td>How a batch went.</td></tr><tr><td><code>GET /api/v1/openapi.json</code></td><td>The whole API as an OpenAPI 3.1 file. No key needed.</td></tr></tbody></table>\n<p>The smallest record:</p>\n<pre><code>{\n  &quot;external_id&quot;: &quot;A-1001&quot;,\n  &quot;title&quot;: &quot;Blue bike&quot;,\n  &quot;description&quot;: &quot;A good bike, 21 gears.&quot;,\n  &quot;category&quot;: &quot;Vehicles &gt; Bikes&quot;\n}</code></pre>\n<p>The <code>external_id</code> is your id. Send the same id again and the plugin updates the listing it made before. Send the same record again and nothing changes. There is no partial update: always send the whole record.</p>\n<p>An id in an address cannot hold a slash. Encode other characters as usual (<code>A%201001</code>).</p>\n<p>Other fields, all optional:</p>\n<ul><li><code>category</code>: an id, a slug, a path, or <code>{&quot;label&quot;: &quot;Bikes&quot;}</code>. A source can set a default.</li><li><code>price</code>: <code>{&quot;amount&quot;: &quot;1.234,50&quot;, &quot;currency&quot;: &quot;EUR&quot;}</code>.</li><li><code>location</code>: <code>country</code>, <code>region</code>, <code>city</code>, <code>city_area</code>, <code>address</code>, <code>zip</code>, <code>lat</code>, <code>lng</code>. Names are matched to the site&#039;s own places.</li><li><code>contact</code>: <code>name</code>, <code>email</code>, <code>phone</code>, <code>show_email</code>.</li><li><code>owner</code>: <code>{&quot;email&quot;: &quot;seller@example.com&quot;}</code> or <code>{&quot;user_id&quot;: 12}</code> for an account on the site. Used only when the source allows records to choose accounts (off by default); otherwise ignored with a warning.</li><li><code>images</code>: one public <code>http</code>/<code>https</code> address, or a list of up to 20. JPEG, PNG, GIF or WebP, 8 MB each. The first 10 are fetched; the rest come back as a warning.</li><li><code>fields</code>: custom field slug → value.</li><li><code>title</code> and <code>description</code> may be per language: <code>{&quot;en_US&quot;: &quot;Bike&quot;, &quot;de_DE&quot;: &quot;Fahrrad&quot;}</code>.</li><li><code>expires_at</code>, <code>published_at</code>: dates. <code>source_url</code>: the listing on your side.</li></ul>\n<p>A field the plugin does not know comes back as a warning. The record is still imported.</p>\n<p>A batch answers at once with a run id:</p>\n<pre><code>curl -X POST https://example.com/api/v1/listings:batch \\\n  -H &quot;Authorization: Bearer $KEY&quot; -H &quot;Content-Type: application/json&quot; \\\n  -d &#039;{&quot;records&quot;: [ ... ]}&#039;</code></pre>\n<pre><code>{&quot;data&quot;:{&quot;run_id&quot;:42,&quot;records&quot;:150,&quot;status&quot;:&quot;queued&quot;}}</code></pre>\n<p>The site&#039;s background jobs import the records. Check <code>GET /api/v1/runs/42</code> for the result.</p>\n<h2>Errors and limits</h2>\n<p>Every error has the same shape:</p>\n<pre><code>{&quot;error&quot;:{&quot;code&quot;:&quot;not_imported&quot;,&quot;message&quot;:&quot;The record was not imported; see fields.&quot;,&quot;fields&quot;:{&quot;title&quot;:&quot;Required.&quot;}}}</code></pre>\n<table><thead><tr><th>Status</th><th>Code</th><th>Why</th></tr></thead><tbody><tr><td>400</td><td><code>invalid_json</code></td><td>The body is not JSON.</td></tr><tr><td>401</td><td><code>unauthorized</code></td><td>No key, or a wrong one.</td></tr><tr><td>403</td><td><code>forbidden</code></td><td>The key lacks the permission.</td></tr><tr><td>404</td><td><code>not_found</code></td><td>No such endpoint, listing or run.</td></tr><tr><td>405</td><td><code>method_not_allowed</code></td><td>The endpoint does not take that method.</td></tr><tr><td>409</td><td><code>no_source</code></td><td>The key&#039;s source is missing or switched off.</td></tr><tr><td>413</td><td><code>too_large</code>, <code>too_many_records</code></td><td>Body over 1 MB, or more than 200 records.</td></tr><tr><td>415</td><td><code>unsupported_media_type</code></td><td>Send <code>Content-Type: application/json</code>.</td></tr><tr><td>422</td><td><code>not_imported</code>, <code>invalid_batch</code>, <code>id_mismatch</code></td><td>The record is wrong. <code>fields</code> says where.</td></tr><tr><td>429</td><td><code>rate_limited</code></td><td>Too many requests. Wait for <code>Retry-After</code> seconds.</td></tr><tr><td>500</td><td><code>not_deleted</code>, <code>server_error</code></td><td>Something failed on the site. The site&#039;s error log says what.</td></tr></tbody></table>\n<p>Each key may send 60 requests a minute. Change it on the plugin&#039;s settings page. An address that sends a wrong key 20 times in 15 minutes is refused for a while.</p>\n<p>The site&#039;s own rules apply to every listing: moderation, spam checks, listing limits, expiry.</p>\n<h2>Pull a feed</h2>\n<p><strong>Plugins → Listing import → Sources</strong>, then the <strong>+</strong> button. Give the feed address, its format, and how often to fetch it. Formats: JSON, NDJSON, CSV, or another Shopclass site&#039;s RSS, up to 5,000 records and 20 MB. A JSON feed may be a plain list or an object holding one list, such as <code>{&quot;products&quot;: [...]}</code>.</p>\n<p>If the feed names its fields differently, map them one per line:</p>\n<pre><code>id = external_id\nHeadline = title\nTown = location.city\nCost = price.amount</code></pre>\n<p><strong>Preview</strong> shows what a fetch would import, without changing anything. <strong>Fetch now</strong> queues a fetch at once.</p>\n<p>A listing whose record leaves the feed is deactivated, not deleted. It comes back when the record returns. If most records in a fetch fail, nothing is deactivated.</p>\n<h2>Import a file</h2>\n<p><strong>Plugins → Listing import → Import a file.</strong> Upload a JSON, NDJSON, CSV or RSS file, up to 20 MB and 5,000 records, and choose its source. The preview shows how each record would be imported. Nothing changes until you click <strong>Import</strong>. Listings the file leaves out are not touched.</p>\n<h2>Command line</h2>\n<pre><code>php oc-cli.php import:run --file=records.json --dry-run   # check a file, change nothing\nphp oc-cli.php import:run --file=records.ndjson           # import it now\nphp oc-cli.php import:status                              # the latest runs\nphp oc-cli.php jobs:work                                  # run queued batches and feeds now</code></pre>\n<p>Batches and feeds run with the site&#039;s background jobs. Make sure the site&#039;s cron runs.</p>\n<h2>Licence</h2>\n<p>GPL-3.0-or-later. See <code>LICENSE</code>.</p>",
    "downloads": 5,
    "funding": null,
    "homepage": "https://github.com/mindstellar/shopclass-plugin-listing-import",
    "icon": "https://raw.githubusercontent.com/mindstellar/shopclass-plugin-listing-import/main/assets/icon.svg",
    "latest_version": "0.2.0",
    "license": "GPL-3.0-or-later",
    "name": "Listing Import",
    "requires_min": "6.4.0",
    "screenshots": [
        {
            "caption": "Sources: partners who send listings, and feeds this site fetches",
            "src": "https://raw.githubusercontent.com/mindstellar/shopclass-plugin-listing-import/main/assets/screenshot-1.png"
        },
        {
            "caption": "Fetch a JSON, CSV or RSS feed on a schedule",
            "src": "https://raw.githubusercontent.com/mindstellar/shopclass-plugin-listing-import/main/assets/screenshot-2.png"
        },
        {
            "caption": "Preview a feed before it changes anything",
            "src": "https://raw.githubusercontent.com/mindstellar/shopclass-plugin-listing-import/main/assets/screenshot-3.png"
        },
        {
            "caption": "API keys with their own permissions",
            "src": "https://raw.githubusercontent.com/mindstellar/shopclass-plugin-listing-import/main/assets/screenshot-4.png"
        },
        {
            "caption": "A help screen with the API guide",
            "src": "https://raw.githubusercontent.com/mindstellar/shopclass-plugin-listing-import/main/assets/screenshot-5.png"
        }
    ],
    "short_description": "Import listings from other systems through a keyed REST API or scheduled JSON, CSV and RSS feeds.",
    "slug": "listing-import",
    "source": "external",
    "support": {
        "docs": "https://github.com/mindstellar/shopclass-plugin-listing-import#readme",
        "issues": "https://github.com/mindstellar/shopclass-plugin-listing-import/issues"
    },
    "tags": [
        "api",
        "csv",
        "feed",
        "import",
        "rss"
    ],
    "tested_max": "6.4",
    "type": "plugin",
    "updated_at": "2026-09-25T06:59:29Z",
    "versions": [
        {
            "changelog_html": "<h3>New</h3>\n<ul><li>Import a file: upload a JSON, NDJSON, CSV or RSS file, preview it, then import it into a source.</li><li>A Help screen with a full record, curl examples, batches, runs and every error code.</li><li>The <code>images</code> field takes one address on its own as well as a list.</li><li>A category written as digits, as in CSV, is read as an id.</li></ul>\n<h3>Changed</h3>\n<ul><li>The admin screens follow core&#039;s layout: one menu group under Plugins, and a <strong>?</strong> help on each screen.</li><li>Listings are saved, keys rate-limited and addresses checked through core&#039;s own tools instead of the plugin&#039;s copies.</li></ul>",
            "downloads": 2,
            "published_at": "2026-09-25T06:59:29Z",
            "requires": "6.4.0",
            "requires_php": "8.0",
            "sha256": "649ead0d84fa166671f5f5f3a602bead75c60b3307a08890113daee3392571ac",
            "size": 107132,
            "tested": "6.4",
            "url": "https://github.com/mindstellar/shopclass-plugin-listing-import/releases/download/v0.2.0/listing-import_0.2.0.zip",
            "version": "0.2.0"
        },
        {
            "changelog_html": "<h3>New</h3>\n<ul><li>API keys: an admin makes, rotates and revokes them under <strong>Plugins → Listing import → API keys</strong>. The secret is shown once and stored only as a hash.</li><li>Every endpoint but ping needs a key with the right permission, and each key is limited to a number of requests a minute.</li><li>An address that fails the key check 20 times in 15 minutes is refused for a while.</li><li><code>POST /api/v1/listings</code> imports one record: it creates a listing, updates the one it made before, or changes nothing when the record is the same.</li><li>Records are placed on the site: category by id, slug, path or name; country, region and city by name; owner by id or e-mail; prices such as &quot;1.234,50&quot; read correctly.</li><li>Imported listings go through core&#039;s own listing save, so validation, spam checks, expiry and custom fields apply. The site&#039;s moderation and listing limits apply too.</li><li>A record may put its listing in a site account only when its source allows it; off by default.</li><li>Images: each address is downloaded only if every IP its host resolves to is public, on the normal port, over http or https, redirects included. At most 8 MB, and only JPEG, PNG, GIF or WebP.</li><li>A host with a dead address is tried at up to three of its checked addresses, for images and feeds.</li><li>An update adds only images the listing does not already have, compared by content, not by address. An image that fails is a warning; the listing is still imported.</li><li>Downloaded images nothing took are swept from the temp folder after two hours.</li><li><code>POST /api/v1/listings:batch</code> takes up to 200 records and answers at once; each record becomes one job in core&#039;s background queue, so core retries it and <strong>Tools → Background jobs</strong> shows it.</li><li><code>GET /api/v1/runs/{id}</code> says how a run went: its counts, whether it is finished, and each failed record with its reasons. A key sees only its own source&#039;s runs.</li><li><code>php oc-cli.php import:run --file=</code> imports a JSON or NDJSON file at once, with <code>--dry-run</code> to change nothing; <code>import:status</code> lists recent runs; <code>import:key:create</code> makes a key.</li><li>Pulled feeds: a source can fetch a JSON, NDJSON or CSV feed, or another Shopclass site&#039;s RSS, every so many minutes. Field names that differ from ours are mapped one per line.</li><li>A JSON feed may wrap its list in an object, such as <code>{&quot;products&quot;: [...]}</code>.</li><li>An image the site would refuse, by type or size, is skipped with a warning instead of failing the listing.</li><li>A listing whose record leaves its feed is deactivated, never deleted, and comes back when the record returns. A feed where most records fail deactivates nothing.</li><li>Sources are managed under <strong>Plugins → Listing import → Sources</strong>: a list, an editor core draws from the plugin&#039;s declaration, a preview of what a feed would import, and Fetch now.</li><li>A feed address must pass the same public-address check as images, and a feed declaring an XML entity is refused before it is parsed.</li><li>Import log lines older than the retention setting are deleted once a day.</li><li>Every request is a run with its own log lines, and a record that fails names each problem by its field.</li><li>The plugin installs its five tables through core&#039;s migration runner and removes them on uninstall.</li><li><code>PUT</code>, <code>GET</code> and <code>DELETE /api/v1/listings/{external_id}</code> replace, read and delete one listing by your id.</li><li><code>GET /api/v1/openapi.json</code> describes the whole API in OpenAPI 3.1.</li><li><code>GET /api/v1/ping</code> answers with the plugin&#039;s version.</li><li>A settings page for the request limit per key and how long import logs are kept.</li></ul>",
            "downloads": 3,
            "published_at": "2026-09-24T12:29:18Z",
            "requires": "6.4.0",
            "requires_php": "8.0",
            "sha256": "f24b62d6100a3300b94ae5548784cb978a3588aa6620ceb1ee233041f38d0447",
            "size": 98595,
            "tested": "6.4",
            "url": "https://github.com/mindstellar/shopclass-plugin-listing-import/releases/download/v0.1.0/listing-import_0.1.0.zip",
            "version": "0.1.0"
        }
    ]
}
