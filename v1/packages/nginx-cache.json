{
    "author": "Mindstellar Community",
    "author_uri": "https://mindstellar.com",
    "categories": [
        "admin-tools"
    ],
    "description_html": "<h1>nginx Cache</h1>\n<p>Hold pages in nginx&#039;s FastCGI cache for an hour instead of thirty seconds, and purge them the moment a listing changes.</p>\n<blockquote><p><strong>Status: complete as a plugin.</strong> Purging, the URL set, the retry queue, the self-test and both admin pages work, verified end to end against nginx 1.31.3 with <code>ngx_cache_purge</code>. What is left is packaging: the Docker image that carries the module, and a catalogue entry.</p></blockquote>\n<h2>What it is for</h2>\n<p>Shopclass sets <code>Cache-Control: public, s-maxage=30</code> on public pages, and nginx honours it. Thirty seconds is short because nothing can invalidate an entry — time is the only eviction. That means the origin re-renders every URL every thirty seconds for as long as anyone is looking at it.</p>\n<p>This plugin lengthens the window and takes responsibility for correctness instead: when a listing changes, the pages showing it are purged immediately.</p>\n<p>What it does <strong>not</strong> buy is page speed: with <code>use_stale updating</code> + <code>background_update on</code>, an expiring entry is already served stale in under a millisecond while it refreshes behind the request. The saving is origin renders — one per URL per hour instead of one per thirty seconds.</p>\n<h2>What it requires</h2>\n<p>nginx built with <code>ngx_cache_purge</code>. That module is not in the official image; it builds cleanly as a <strong>dynamic</strong> module against current nginx (verified on 1.31.3) in a two-stage Dockerfile, which is stock nginx plus one module rather than a switch to OpenResty. The plugin&#039;s <strong>Setup</strong> page prints the Dockerfile, the <code>load_module</code> line and the purge <code>location</code> block for this install.</p>\n<p>The bundled Shopclass Docker image ships it preconfigured. A standalone install adds it by hand.</p>\n<h2>Safety</h2>\n<p>The longer window is <strong>gated on a self-test</strong>. Until the plugin has primed a URL, purged it, and confirmed the entry is gone, it serves core&#039;s own thirty seconds. A long window with a purge that quietly does not work is worse than no plugin at all — the failure is invisible and lasts an hour — so it is not something the plugin will do on trust. The flag clears whenever the endpoint or host changes.</p>\n<h2>What it purges, and what it does not</h2>\n<p>Purged, because their URLs can be named: the listing itself and one per locale, the homepage, the listing&#039;s category page, and the seller&#039;s public profile.</p>\n<p>Not purged, and so left on core&#039;s short window:</p>\n<ul><li><strong>search results carrying parameters</strong> — every keyword, filter, sort and page number is</li></ul>\n<p>  its own cache entry, the set cannot be enumerated, and a newly posted listing has to   appear in them;</p>\n<ul><li><strong>any URL with a query string</strong>, including <code>?comments-page=2</code> on a listing and</li></ul>\n<p>  <code>?utm_source=…</code> on the home page. Each is a separate cache entry that no purge names;</p>\n<ul><li><strong>every page, if permalinks are off</strong>, since the canonical URL of each is then a query</li></ul>\n<p>  URL itself.</p>\n<p>The rule behind all three: a page is held longer only when the URL being served is the one a purge will name.</p>\n<h2>Events it listens to</h2>\n<p>The set the Cloudflare plugin uses, which is the tested list of what core actually fires, plus one more:</p>\n<p><code>posted_item</code>, <code>edited_item</code>, <code>after_delete_item</code>, <code>enable_item</code>, <code>disable_item</code>, <code>activate_item</code>, <code>deactivate_item</code>, <code>item_premium_on</code>, <code>item_premium_off</code>, <code>item_expiration_updated</code>, <code>add_category</code>, <code>after_delete_category</code>, <code>edit_page</code>, <code>after_delete_page</code>, and <strong><code>invalidate_item_cache</code></strong>.</p>\n<p>That last one (Shopclass 6.2.0+) is what carries a completed <strong>storage offload</strong>. An offload rewrites a listing&#039;s image URLs and fires nothing else, so without it a cached page goes on pointing at local files that have been moved to the remote — a gap the Cloudflare plugin has too.</p>\n<h2>Relationship to the Cloudflare plugin</h2>\n<p>They compose. <code>shopclass-plugin-cloudflare</code> owns the edge; this owns the origin. Both hang off the same core hooks and share no code. Run either or both.</p>\n<h2>Configuration</h2>\n<p>Two pages under <strong>Plugins</strong>: the settings, and a <strong>Setup</strong> page that prints the nginx configuration this install needs — its own host, its own scheme, its own nginx version — rather than a sample to adapt. The same config is in <a href=\"nginx/shopclass-cache.conf\" rel=\"nofollow noopener noreferrer\" target=\"_blank\"><code>nginx/shopclass-cache.conf</code></a> for reading outside the admin.</p>\n<table><thead><tr><th>Preference (<code>nginx_cache</code> section)</th><th>Default</th></tr></thead><tbody><tr><td><code>purge_endpoint</code></td><td><code>$SHOPCLASS_PURGE_ENDPOINT</code>, else <code>&lt;nginx&#039;s scheme&gt;://127.0.0.1/purge</code></td></tr><tr><td><code>purge_host</code></td><td><code>$SHOPCLASS_PURGE_HOST</code>, else the site&#039;s host and port. One per line — see below</td></tr><tr><td><code>ttl_item</code>, <code>ttl_page</code>, <code>ttl_aggregate</code></td><td>3600, capped at 3600</td></tr></tbody></table>\n<p>Both halves of the endpoint matter and neither is obvious: the request must reach the origin <strong>and</strong> present the host and scheme the cache key was built with. <strong>Test purge</strong> proves that combination rather than trusting it, and changing either setting closes the gate again.</p>\n<p><strong>List every hostname the site answers on.</strong> nginx files a separate copy of each page under each <code>Host</code> it was asked with, so a name left out goes on serving what it already had for the whole window — <code>www.example.com</code> when the site is configured as <code>example.com</code>, an alias, a staging domain. Test purge primes and purges each one in turn, because priming with <code>Host: X</code> files an entry under X and can therefore prove it. The one thing it cannot prove is that those names are what visitors send, so it requires the site&#039;s own host to be among them: a list of typos would otherwise verify itself perfectly and purge nothing anybody reads.</p>\n<p>The 3600 cap is not arbitrary. A cached page carries the CSRF token minted when it was stored, and core stops accepting one 7200s after it was issued — so a longer window has forms on cached pages answering &quot;your session has expired&quot; while purging goes on working perfectly. Going past it means taking the token out of the HTML, which is a decision about requiring JavaScript to submit forms, not a caching setting.</p>\n<h2>Tests</h2>\n<p><code>./tests/run.sh</code> — standalone, no database and no running site.</p>\n<h2>Extending</h2>\n<table><thead><tr><th>Filter</th><th>Purpose</th></tr></thead><tbody><tr><td><code>nginx_cache_item_urls</code></td><td>add URLs for a theme&#039;s own item routes</td></tr><tr><td><code>nginx_cache_purge_urls</code></td><td>take the whole list and deliver it somewhere else</td></tr></tbody></table>\n<h2>Licence</h2>\n<p>GPL-3.0-or-later. © Mindstellar Community.</p>",
    "downloads": 11,
    "funding": null,
    "homepage": "https://github.com/mindstellar/shopclass-plugin-nginx-cache",
    "icon": "https://raw.githubusercontent.com/mindstellar/shopclass-plugin-nginx-cache/main/assets/icon.svg",
    "latest_version": "0.4.0",
    "license": "GPL-3.0-or-later",
    "name": "nginx Cache",
    "requires_min": "6.2.0",
    "screenshots": [],
    "short_description": "Hold pages in nginx's FastCGI cache for an hour instead of thirty seconds, and purge them the moment a listing changes.",
    "slug": "nginx-cache",
    "source": "external",
    "support": {
        "docs": "https://github.com/mindstellar/shopclass-plugin-nginx-cache#readme",
        "issues": "https://github.com/mindstellar/shopclass-plugin-nginx-cache/issues"
    },
    "tags": [
        "cache",
        "fastcgi",
        "nginx",
        "performance",
        "purge"
    ],
    "tested_max": "6.2",
    "type": "plugin",
    "updated_at": "2026-08-28T17:28:54Z",
    "versions": [
        {
            "changelog_html": "<h3>New</h3>\n<ul><li>The purge host is a list: a site answering on several names has each one purged, and</li></ul>\n<p>  Test purge proves each in turn.</p>\n<h3>Fixed</h3>\n<ul><li>A site whose visitors arrive on a name other than the configured one — <code>www</code>, an alias,</li></ul>\n<p>  a proxy passing a different Host — verified against a host nobody uses while every real   cached page went unpurged.</p>",
            "downloads": 5,
            "published_at": "2026-08-28T17:28:54Z",
            "requires": "6.2.0",
            "requires_php": "8.0",
            "sha256": "327f58554f8eed8749d782a7c24fb0717de0f69c42d0142d44a14d1a83cbc956",
            "size": 45569,
            "tested": "6.2",
            "url": "https://github.com/mindstellar/shopclass-plugin-nginx-cache/releases/download/v0.4.0/nginx-cache_0.4.0.zip",
            "version": "0.4.0"
        },
        {
            "changelog_html": "<h3>New</h3>\n<ul><li>Settings page: endpoint, host, the three windows, <strong>Test purge</strong>, and the queue.</li><li>Setup page printing the nginx configuration this install needs, filled in with its own</li></ul>\n<p>  host, scheme and nginx version.</p>\n<h3>Changed</h3>\n<ul><li>Windows are capped at 3600s — past that the CSRF token cached inside a page outlives</li></ul>\n<p>  itself and every form on it starts reporting an expired session.</p>\n<ul><li>The self-test counts STALE, UPDATING and EXPIRED as cached; requiring HIT failed it</li></ul>\n<p>  whenever an entry happened to be refreshing.</p>",
            "downloads": 6,
            "published_at": "2026-08-28T16:46:46Z",
            "requires": "6.2.0",
            "requires_php": "8.0",
            "sha256": "345880b5c36af883ac207610b423ec04515756bfefe4a921223999faea788dd1",
            "size": 43725,
            "tested": "6.2",
            "url": "https://github.com/mindstellar/shopclass-plugin-nginx-cache/releases/download/v0.3.0/nginx-cache_0.3.0.zip",
            "version": "0.3.0"
        }
    ]
}
