{
    "author": "Mindstellar Community",
    "author_uri": "https://mindstellar.com",
    "categories": [
        "content-widgets",
        "integrations"
    ],
    "description_html": "<h1>QR Code</h1>\n<p>Puts a QR code on each listing, so it can be scanned from a screen, shared, or printed and put in a window.</p>\n<h2>How it works</h2>\n<p>The code is drawn <strong>in the browser</strong>, as inline SVG, and only once a visitor asks for it. Nothing is stored on the server.</p>\n<p>That is the whole design, and it is what makes the rest fall out:</p>\n<ul><li><strong>No files.</strong> The original rendered a PNG per listing into <code>oc-content/uploads/qrcode/</code></li></ul>\n<p>  and served it. A QR code is a pure function of a URL, so storing one buys nothing and   costs a growing directory, a cleanup pass on delete and on uninstall, and an orphaned   file every time a permalink changes.</p>\n<ul><li><strong>No GD, no PHP encoder.</strong> The encoder is 52 KB of JavaScript fetched on demand — a</li></ul>\n<p>  listing nobody scans downloads none of it.</p>\n<ul><li><strong>Sharp at any size.</strong> SVG, not a fixed-resolution bitmap, which is what a printed code</li></ul>\n<p>  wants.</p>\n<h2>What the visitor gets</h2>\n<p>A <strong>Show QR code</strong> button opens a native dialog with the code and the listing address, and:</p>\n<table><thead><tr><th>Action</th><th>When it appears</th></tr></thead><tbody><tr><td><strong>Share</strong></td><td>where the browser has the Web Share API — hands off to the system share sheet</td></tr><tr><td><strong>Copy link</strong></td><td>where the browser has the async Clipboard API</td></tr><tr><td><strong>Print</strong></td><td>always; prints the code and the address alone, at 60 mm</td></tr></tbody></table>\n<p>The two conditional buttons are hidden until support is confirmed, rather than shown and failing.</p>\n<h2>Settings</h2>\n<p>One: the error-correction level, under Plugins → QR Code → Configure. <strong>M</strong> is the default — it survives a scuffed print or an off-angle scan while keeping the symbol sparse enough to read quickly. <strong>H</strong> costs roughly a third more modules for damage tolerance most listings never need.</p>\n<h2>Where it appears</h2>\n<p>Through the <code>item_detail</code> theme hook. The original defined <code>show_qrcode()</code> and left it to the theme to call, so on any theme that had not been edited for it, the plugin did nothing.</p>\n<h2>Requirements</h2>\n<p>Shopclass 6.1.0 or newer, PHP 8.0 or newer. The browser needs ES modules and dynamic <code>import()</code> — every browser released since 2018.</p>\n<h2>Third-party code</h2>\n<p><code>vendor/qrcode.js</code> and <code>vendor/qrcode-utf8.js</code> are the QR Code Generator for JavaScript by Kazuhiko Arase, MIT licensed, included verbatim. See <code>vendor/LICENSE</code>.</p>\n<p>The UTF-8 companion is not optional: left on its default the encoder writes one byte per code unit, and a URL containing anything outside ASCII produces a symbol that fails to scan or decodes to the wrong text.</p>\n<h2>History</h2>\n<p>Derived from the Osclass &quot;QR Codes&quot; plugin (1.0.3, 2013). See CHANGELOG.md.</p>\n<h2>Licence</h2>\n<p>GPL-3.0-or-later. Derived from Osclass, originally Apache-2.0; both notices are retained in the source headers.</p>",
    "downloads": 0,
    "funding": null,
    "homepage": "https://github.com/mindstellar/shopclass-plugins",
    "icon": null,
    "latest_version": null,
    "license": "GPL-3.0-or-later",
    "name": "QR Code",
    "requires_min": null,
    "screenshots": [],
    "short_description": "Show a QR code for a listing so it can be scanned, shared or printed.",
    "slug": "qr-code",
    "source": "in-repo",
    "support": {
        "docs": "https://github.com/mindstellar/shopclass/blob/develop/docs/PACKAGE-SPEC.md",
        "issues": "https://github.com/mindstellar/shopclass-plugins/issues"
    },
    "tags": [
        "listing",
        "offline",
        "print",
        "qr",
        "share"
    ],
    "tested_max": null,
    "type": "plugin",
    "updated_at": null,
    "versions": []
}
